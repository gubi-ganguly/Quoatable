# Microsoft Authentication Library for OAuth
msal>=1.34.0

# HTTP requests library
requests>=2.32.0

# FastAPI web framework
fastapi>=0.109.0

# ASGI server for FastAPI
uvicorn[standard]>=0.27.0

# Pydantic for data validation (included with FastAPI, but explicit for clarity)
pydantic>=2.5.0
pydantic-settings>=2.0.0

# Email validation for Pydantic
email-validator>=2.1.0

# Python dotenv for environment variable management (optional but useful)
python-dotenv>=1.0.0
